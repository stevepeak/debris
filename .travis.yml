language: python
python:
  - 2.7
  - pypy
services: 
  - postgres
  - redis
  - memcached
before_script:
  - pip install Tornado python-binary-memcached coveralls
script:
  - coverage run -m unittest discover -s tests
after_success:
  coveralls
